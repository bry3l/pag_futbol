# Project instructions

Use Claude Code with these project roles:

- `developer`: implement and maintain application code.
- `tester`: create, run, and maintain tests; reproduce and verify bugs.

Keep changes focused, run the relevant tests after edits, and report commands that could not be run.
